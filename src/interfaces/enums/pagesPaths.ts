enum pagesPaths {
    MainPage = '/mainPage',
    Profile = '/profile/:serialNumber',
    AllUsers = '/allUsers',
    AdmissionReport = '/admissionReport',
    Reports = '/reports',
}

export default pagesPaths